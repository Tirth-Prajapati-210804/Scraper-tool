# Local dev — Vite proxies nothing, so point directly at the backend dev server.
# Leave empty (or remove this file) when building the Docker image; nginx handles
# the proxy from /api/ → backend:8000 automatically.
VITE_API_BASE_URL=http://localhost:8000
